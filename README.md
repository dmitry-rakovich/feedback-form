### Для запуска:
- скопировать репозиторий `git clone https://github.com/dmitry-rakovich/feedback-form.git`
- перейти в папку `cd feedback-form`
- установить зависимости `npm install`
- запустить в отдельных терминала тестовый сервер `npm run server` и сам проект `npm start`
